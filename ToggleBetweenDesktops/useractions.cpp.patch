--- kde-workspace-4.7.3/kwin/useractions.cpp	2011-05-20 23:32:07.000000000 +0300
+++ kde-workspace-4.7.3.modified/kwin/useractions.cpp	2011-11-19 09:05:16.000000000 +0200
@@ -1166,6 +1166,14 @@ void Workspace::slotSwitchDesktopDown()
     setCurrentDesktop(desktop);
 }
 
+void Workspace::slotToggleBetweenDesktops()
+{
+    kDebug() << " Current desktop: " << currentDesktop_ << "\n";
+    kDebug() << "Previous desktop: " << previousDesktop_ << "\n";
+    
+    setCurrentDesktop(previousDesktop_);
+}
+
 static int senderValue(QObject *sender)
 {
     QAction *act = qobject_cast<QAction*>(sender);
